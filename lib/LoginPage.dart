import 'package:flutter/material.dart';
import 'package:tech_tinder/SignIn.dart';

class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SignIn(),
    );
  }
}
